- id: k8s-yaml-schema
  name: K8s YAML schema directive
  description: Inserts or updates yaml-language-server $schema directives from apiVersion/kind.
  entry: k8s-yaml-schema
  language: python
  files: \.ya?ml$
  args: [--config, .k8s-schema-hook.yaml]
